# ==============================================
# AI Agent 环境配置模板
# 复制此文件为 .env 并填入实际值
#
# 路径配置规则:
#   留空 / 注释掉 → 使用项目默认路径 (AiAgent 目录下)
#   绝对路径       → 直接使用 (部署环境推荐，如 D:\data\kb 或 /data/kb)
#   相对路径       → 基于 AiAgent 根目录解析
# ==============================================

# ---- 通义千问 LLM ----
DASHSCOPE_API_KEY=your_dashscope_api_key_here
QWEN_MODEL_NAME=qwen-plus
LLM_TEMPERATURE=0.3
LLM_TOP_P=0.8

# ---- C++ 仿真引擎 HTTP 接口 ----
SIM_ENGINE_HOST=localhost
SIM_ENGINE_PORT=8080
SIM_ENGINE_HTTP_TIMEOUT=10.0

# ---- Agent 行为 ----
AGENT_MAX_ITERATIONS=50
AGENT_TIMEOUT_SECONDS=30

# ---- RAG 知识检索 ----
RAG_EMBEDDING_MODEL=text-embedding-v3
RAG_CHUNK_SIZE=800
RAG_CHUNK_OVERLAP=100
RAG_RETRIEVE_TOP_K=3
# 目录路径: 留空则使用默认路径，支持绝对/相对路径
# RAG_KNOWLEDGE_BASE_DIR=          # 默认: AiAgent/rag/knowledge_base
# RAG_VECTOR_STORE_DIR=            # 默认: AiAgent/rag/vector_store

# ---- 日志 ----
LOG_LEVEL=INFO
LOG_FILE_LEVEL=DEBUG
LOG_ROTATION=50 MB
LOG_RETENTION=7 days
# LOG_DIR=                         # 默认: AiAgent/logs

# ---- 回放记录 ----
# REPLAY_DIR=                      # 默认: AiAgent/replays
